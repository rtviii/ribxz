import { request } from 'http'
import path from 'path'
import fs from 'fs'


export const inspect=(requested:string, prottype:string)=>{
    const pdbid = requested.toUpperCase() + '.json'
    // const structfile = openMain(requested)
    // console.log(structfile);
    

}